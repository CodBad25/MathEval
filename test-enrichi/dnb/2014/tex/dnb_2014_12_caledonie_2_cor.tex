
\begin{enumerate}
\item Je joue une partie face à  un adversaire qui joue au hasard et je choisis de jouer \og \textbf{pierre} \fg{}. 
	\begin{enumerate}
		\item Je perds la partie si mon adversaire choisit \og \textbf{feuille} \fg{} parmi les trois  possibilités \og \textbf{pierre} \fg{}, \og \textbf{ciseaux} \fg{} et \og \textbf{feuille} \fg{}, donc la probabilité que je perde la partie est égale à $\dfrac13$.
		\item La probabilité que je ne perde pas la partie est égale à $\dfrac23$. \quad Car $\left( 1-\dfrac13=\dfrac23\right)$
	\end{enumerate}
`\item  Arbre des possibles de l'adversaire pour les deux parties. 

% Set the overall layout of the tree
\tikzstyle{level 1}=[level distance=3.5cm, sibling distance=5.5cm]
\tikzstyle{level 2}=[level distance=3.5cm, sibling distance=2cm]

% Define styles for bags and leafs
\tikzstyle{bag} = [text width=4em, text centered]
\tikzstyle{end} = [minimum width=3pt, inner sep=0pt]

\begin{tikzpicture}[grow=right, sloped]
\node (Root) [bag] {}
    child {
        node[bag] {$C$}        
            child {
                node[end, label=right:
                    {$C \qquad  P(C \cap C)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, label=right:
                    {$F \qquad  P(C \cap F)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, label=right:
                    {$P \qquad P(C \cap P)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            edge from parent[-{Stealth[length=2mm]}] 
            node[above] {}
            node[fill,color=white,text=black] {$\frac{1}{3}$}
    }
    child {
        node[bag] {$F$}        
            child {
                node[end, label=right:
                    {$C \qquad P(F \cap C)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, label=right:
                    {$F \qquad  P(F \cap F)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, label=right:
                    {$P \qquad  P(F \cap P)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            edge from parent[-{Stealth[length=2mm]}] 
            node[above] {}
            node[fill,color=white,text=black] {$\frac{1}{3}$}
    }
    child {
        node[bag, name=premierLance] {$P$}        
        child {
                node[end, label=right:
                    {$C \qquad P(P \cap P)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, label=right:
                    {$F \qquad P(P \cap F)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            child {
                node[end, name=deuxLance, label=right:
                    {$P \qquad P(P \cap P)=\frac{1}{3}\times\frac{1}{3}= \frac{1}{9}$}] {}
                edge from parent[-{Stealth[length=2mm]}]
                node[fill,color=white,text=black] {$\frac{1}{3}$}
            }
            edge from parent[-{Stealth[length=2mm]}] 
            node[above] {}
            node[fill,color=white,text=black] {$\frac{1}{3}$}
    };
    \begin{scope}[every node/.style={above}]
 	\path (premierLance    -| premierLance)  ++(0,25mm) node {1\up{re} partie};
 	\path (deuxLance    -| deuxLance)  ++(0,05mm) node {2\up{de} partie};
 	\path (deuxLance    -| deuxLance)  ++(3,05mm) node {Probabilité};
	\end{scope}
\end{tikzpicture}


\item  	\begin{enumerate}
`		\item Je gagne les deux parties si mon adversaire choisit \og \textbf{ciseaux} \fg{} aux deux  parties, donc la probabilité que je gagne les deux parties est égale à  $P$(C, C), soit $\dfrac19$. 
		\item Je ne perds aucune des deux parties si mon adversaire choisit \og \textbf{ciseaux} \fg{} ou \og \textbf{pierre} \fg{} dans les parties donc la probabilité que je ne perde aucune des deux parties est égale à $P(\text{P}, \text{P})+P(\text{P}, \text{C})+P(\text{C}, \text{P}) + P(\text{C}, \text{C})=4\times \dfrac13=\dfrac49$. 
	\end{enumerate}
\end{enumerate}

\vspace{0,5cm}



